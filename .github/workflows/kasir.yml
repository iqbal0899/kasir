name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted 
    steps:
      - uses: actions/checkout@v4

      - name: Menjalankan skrip
        run: echo "Halo dari runner self-hosted"
